# ── Nebula Docker Compose ────────────────────────────────────────
#
# 单机开发部署：APISIX Gateway + nebula-server (Rust Axum) + nebula-frontend (React Nginx)
#
# 使用方式：
#   docker compose build
#   docker compose up -d
#   docker compose logs -f
#   docker compose down
#

services:
  # ── API 网关 (Apache APISIX) ────────────────────────────────────
  nebula-gateway:
    image: apache/apisix:3.14.0-debian
    container_name: nebula-gateway
    ports:
      - "${APISIX_HTTP_PORT:-80}:9080"
      # 未来启用 HTTPS 或 Prometheus 时取消注释：
      # - "${APISIX_HTTPS_PORT:-9443}:9443"
      # - "${APISIX_PROMETHEUS_PORT:-9180}:9180"
    volumes:
      - ./docker/apisix/config.yaml:/usr/local/apisix/conf/config.yaml:ro
      - ./docker/apisix/apisix.yaml:/usr/local/apisix/conf/apisix.yaml:ro
    restart: unless-stopped
    depends_on:
      nebula-server:
        condition: service_healthy
      nebula-frontend:
        condition: service_started
    networks:
      - nebula-net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsSo /dev/null http://127.0.0.1:9080/ || exit 1"]
      interval: 15s
      timeout: 3s
      retries: 3
      start_period: 20s

  # ── Rust 后端 ──────────────────────────────────────────────────
  nebula-server:
    build:
      context: .
      dockerfile: docker/Dockerfile.crates
    image: nebula-server:latest
    container_name: nebula-server
    ports:
      - "3030:3030"
    environment:
      - NEBULA_SERVER_ADDR=0.0.0.0:3030
      - RUST_LOG=nebula_ffi=info,tower_http=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3030/health"]
      interval: 15s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - nebula-net

  # ── React 前端 (nginx, 仅静态文件) ─────────────────────────────
  nebula-frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.nebula
    image: nebula-frontend:latest
    container_name: nebula-frontend
    # 不对外暴露端口 — APISIX 作为唯一入口代理所有流量
    restart: unless-stopped
    networks:
      - nebula-net

  # ── pymeep 仿真环境 (Ubuntu 22.04 + Miniconda + pymeep) ──────────
  nebula-mpmcp:
    build:
      context: .
      dockerfile: docker/mpmcp/Dockerfile
    image: nebula-mpmcp:latest
    container_name: nebula-mpmcp
    command: ["sleep", "infinity"]
    ports:
      - "${MPMCP_SSH_PORT:-2222}:22"
    volumes:
      - ./data/Workspace:/home/nebula/Workspace
      - ./data/PDK:/home/nebula/PDK
      - ./data/Examples:/home/nebula/Examples
      - ./.hermes/ssh/nebula-mpmcp-key.pub:/home/nebula/.ssh/authorized_keys:ro
    restart: unless-stopped
    networks:
      - nebula-net

  # ── Hermes AI Agent (Gateway + Dashboard, s6-overlay) ──────────
  hermes-agent:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-agent
    # CMD 作为主进程运行 gateway；s6 dashboard 服务由 HERMES_DASHBOARD=1 激活
    command: gateway run
    stdin_open: true
    tty: true
    environment:
      - HERMES_DASHBOARD=1
      - HERMES_DASHBOARD_HOST=0.0.0.0
      - HERMES_DASHBOARD_PORT=9119
      - HERMES_DASHBOARD_INSECURE=1
    ports:
      - "8642:8642"    # Gateway API
      - "9119:9119"    # Dashboard Web UI
    volumes:
      - ./.hermes:/opt/data
    restart: unless-stopped
    networks:
      - nebula-net

networks:
  nebula-net:
    driver: bridge
    name: nebula-net
